{
  # FIXME: encounter duplicate definition error when update to den 0.12.

  # den.hosts.x86_64-darwin = {
  #   freya.users.hide = { };
  # };

  den.hosts.aarch64-darwin = {
    lenneth.users.ikeno = { };
    # silmeria.users.ikeno = { };
  };
}
